# Claude Code 작업 원칙

- 기존 코드를 먼저 분석한 후 수정한다.
- 요청하지 않은 기능을 임의로 추가하지 않는다.
- 기존 기능을 임의로 삭제하지 않는다.
- 작업 전에 관련 파일을 확인한다.
- 대규모 구조 변경 전에는 사용자에게 확인한다.
- 수정 후 오류와 실행 여부를 확인한다.
- 코드 중복을 최소화한다.
- TypeScript를 기본으로 사용한다.
- 컴포넌트는 재사용성을 고려한다.
- 환경변수와 비밀키를 코드에 직접 작성하지 않는다.
- 불필요한 라이브러리를 추가하지 않는다.
- 작업 완료 후 변경된 파일과 내용을 요약한다.

## 프로젝트 명령어

- `npm run dev`: 개발 서버 실행
- `npm run build`: 타입 검사 및 프로덕션 빌드
- `npm run preview`: 프로덕션 빌드 미리보기
